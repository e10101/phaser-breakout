{
    "name": "coding_test",
    "version": "0.0.1",
    "scripts": {
        "build": "tsc",
        "start": "cd output && http-server -p 8080 --cors"
    },
    "devDependencies": {
        "http-server": "^0.9.0",
        "phaser": "^2.6.2",
        "typescript": "^2.2.1"
    }
}