
export const Position = class {
  public x: number;
  public y: number;
  constructor(x: number, y: number) {
    this.x = x;
    this.y = y;
  }

  seXY (x: number, y: number) {
    this.x = x;
    this.y = y;
  }
}
